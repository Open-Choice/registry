{
  "command": "assess-design",
  "title": "Assess an existing MaxDiff design",
  "summary": "Computes all balance and coverage metrics for a previously generated or externally constructed MaxDiff design.",
  "arguments": [
    {
      "name": "design_path",
      "type": "string",
      "required": true,
      "description": "Path to the wide-format design CSV. Must have columns Version, Task followed by one column per item slot (Item1, Item2, ...) containing 1-based item indices."
    },
    {
      "name": "run_dir",
      "type": "string",
      "required": true,
      "description": "Workspace directory where assessment artifacts will be written."
    },
    {
      "name": "run_name",
      "type": "string",
      "required": true,
      "description": "Run identifier; output files are written to {run_dir}/ocinfo/{run_name}_design/."
    }
  ],
  "value": "Produces a unified assessment with item-exposure balance, pair-coverage, within-version and total SSE metrics for any MaxDiff design, regardless of the objective used to generate it.",
  "notes": [
    "The spec (n_items, items_per_task, tasks_per_version, versions) is inferred from the CSV header and data.",
    "Metrics include per-version and total exposure counts, pair co-occurrence counts, SSE for item and pair balance within versions, and SSE for total item and pair balance across all versions.",
    "pair_counts_nonzero reports how many of the possible item pairs appear at least once."
  ],
  "see_also": [
    {
      "command": "design-maxdiff::design",
      "description": "Generate the MaxDiff design that assess-design scores."
    }
  ],
  "artifacts": [
    {
      "path": "ocinfo/{run_name}_design/summary_overview.csv",
      "title": "Design balance overview",
      "description": "Item-exposure balance, pair-coverage, and SSE metrics. Use these to verify design quality before fielding, or to compare against an alternative design.",
      "fields": []
    }
  ],
  "description": "Run `assess-design` on any MaxDiff design file — whether generated by `design-maxdiff::design` or produced by an external tool — to compute a unified set of balance and coverage metrics.\n\nUse this endpoint as the quality-check step before fielding a MaxDiff survey. A well-balanced design shows each item approximately the same number of times across the full sample, covers as many item pairs as possible, and distributes items evenly within each version.",
  "usage": "[[\"design-maxdiff::assess-design\"]]\ndesign_path = \"./data/maxdiff_design.csv\"\nrun_dir = \"./runs\"\nrun_name = \"assess1\"",
  "details": "The assessment scores four properties of the design. Item-exposure balance measures how evenly each item appears across tasks and versions — a balanced design has each item shown roughly the same total number of times. Positional balance checks whether items appear in each slot (position 1, position 2, …) equally often, to avoid position-order bias. Pair-coverage tracks how many of the possible item pairs co-appear in at least one task — full coverage means every pair is seen together somewhere in the design, giving the model the information it needs to rank every pair. The SSE terms aggregate the within-version and across-version counts into a single quality number per metric.\n\nRunning `assess-design` after generation is a quick sanity check before fielding. It is also useful for evaluating externally supplied designs or comparing two competing designs on a common scale.",
  "examples": [
    {
      "title": "Assess a previously generated MaxDiff design",
      "body": "[[\"design-maxdiff::assess-design\"]]\ndesign_path = \"./data/maxdiff_design.csv\"\nrun_dir = \"./runs\"\nrun_name = \"assess1\""
    }
  ]
}