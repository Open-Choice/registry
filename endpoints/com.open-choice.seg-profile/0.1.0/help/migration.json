{
  "command": "migration",
  "title": "Cross-tabulate segment memberships across solutions",
  "summary": "Builds a single stacked cross-tabulation of segment labels across one or more solution columns in a memberships CSV. Writes a square matrix (total row/col plus one row/col per level of each solution) as a CSV sidecar and an xlsx workbook with merged solution-name headers, a 3-colour scale across the data body, and frozen header panes.",
  "description": "Given a memberships CSV with a respondent id and one or more solution columns (e.g. `sol_3`, `sol_4`), computes `table(sol_i, sol_j)` for every pair and assembles the results into a stacked matrix of size `1 + Σ k_i` where `k_i` is the number of levels in solution i. Row/column 0 is the total count and per-level marginals. Missing values are excluded. Used to see how respondents migrate between competing segmentations, or to confirm the per-level sample sizes of a single solution.",
  "usage": "[[\"seg-profile::migration\"]]\nmemberships = \"<memberships.csv>\"\nrun_dir = \"./runs\"\nrun_name = \"m1\"\n# optional:\nuuid_col = \"resp_id\"\nsolution_cols = [\"sol_3\", \"sol_4\"]",
  "arguments": [
    {
      "name": "memberships",
      "type": "string",
      "required": true,
      "description": "CSV with per-respondent segment labels across one or more solution columns."
    },
    {
      "name": "run_dir",
      "type": "string",
      "required": true,
      "description": "Base directory. Outputs land in `<run_dir>/ocinfo/<run_name>_seg_profile/`."
    },
    {
      "name": "run_name",
      "type": "string",
      "required": true,
      "description": "Short identifier for this run; no '..', '/' or '\\\\'."
    },
    {
      "name": "uuid_col",
      "type": "string",
      "required": false,
      "description": "Respondent identifier column. If omitted, the first column is treated as the id."
    },
    {
      "name": "solution_cols",
      "type": "array",
      "required": false,
      "default": [],
      "description": "Subset of solution columns to include. Empty = use all non-uuid columns."
    },
    {
      "name": "headers",
      "type": "boolean",
      "required": false,
      "default": true,
      "description": "Whether the CSV has a header row."
    },
    {
      "name": "output_format",
      "type": "string",
      "required": false,
      "description": "Override stdout format: `human`, `protocol`, or `quiet`."
    }
  ],
  "notes": [
    "The xlsx uses 3-colour conditional formatting (lavender → white → orange) over the body only, so the total row/col does not dominate the scale.",
    "Diagonal blocks are self-cross-tabs (sol_i × sol_i); off-diagonal blocks are (sol_i × sol_j)."
  ],
  "artifacts": [
    {
      "path": "ocinfo/{run_name}_seg_profile/migration.xlsx",
      "title": "Segment migration workbook",
      "description": "A cross-tabulation matrix showing how respondents are distributed across multiple segmentation solutions. Diagonal blocks show each solution's own segment sizes; off-diagonal blocks show how respondents from one solution's segments map across another's. Used to assess stability between competing segmentations.",
      "fields": []
    },
    {
      "path": "ocinfo/{run_name}_seg_profile/migration.csv",
      "title": "Migration matrix data",
      "description": "The same cross-tabulation in CSV format for further processing.",
      "fields": []
    }
  ],
  "details": "The migration table is a stacked cross-tabulation. Each entry in the matrix is the count of respondents who belong to segment i of solution A and segment j of solution B. Reading across a row tells you where the respondents in one segment map in the comparison solution; reading down a column tells you where the respondents in a comparison-solution segment came from. The total row and column give the marginal counts for each solution independently.\n\nDiagonal blocks (where the row solution and column solution are the same) are self-cross-tabs and show only the per-segment sample sizes. Off-diagonal blocks are the actual migration tables. When you have more than two solutions, all pairwise combinations are stacked into a single workbook sheet, making it easy to scan across a grid of comparisons at once.\n\nThis command is typically used to assess the stability of a segmentation: if a 3-segment solution and a 4-segment solution agree strongly on most respondents, the extra segment in the 4-class solution represents a genuine split rather than noise.",
  "see_also": [
    {
      "command": "seg-profile::profile",
      "description": "Profile segment membership against numeric survey variables — the natural follow-on after reviewing migration patterns."
    }
  ],
  "examples": [
    {
      "title": "Cross-tabulate two competing segmentation solutions",
      "body": "[[\"seg-profile::migration\"]]\nmemberships = \"./output/memberships.csv\"\nrun_dir = \"./runs\"\nrun_name = \"m1\"\nuuid_col = \"resp_id\"\nsolution_cols = [\"sol_3\", \"sol_4\"]"
    }
  ]
}